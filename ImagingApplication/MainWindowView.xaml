<Window x:Class="ImagingApplication.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImagingApplication"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        Title="Imaging Application" Height="350" Width="525">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Content="Perform OCR" Command="{Binding PerformOcrCommand}"/>
            <TextBlock Text="{Binding ProcessedText}"/>
            <Button Content="Find Object in Scene" Command="{Binding FindObjectInSceneCommand}"/>
        </StackPanel>
        
    </Grid>
</Window>
